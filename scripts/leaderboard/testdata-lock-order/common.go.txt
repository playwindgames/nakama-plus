// 每个夹具目录都会内联一份等价的类型声明（检查器只看被扫描到的文件，
// 不做跨目录解析）。这份留作参照，扩展夹具时照抄。
//
// 后缀是 .txt，免得它被任何 Go 工具当成源码收进来。

package fixture

import "sync"

type LeaderboardScheduler interface{ Update() }

type cacheStub struct {
	sync.Mutex
	items map[string]int
}

type holder struct {
	mu                   sync.RWMutex
	leaderboardScheduler LeaderboardScheduler
}
